# Server
PORT=3000
BASE_URL=http://localhost:3000
NODE_ENV=development

# Email (SMTP) — leave blank to use dev-mode magic links shown on screen
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="School Calendar <your@gmail.com>"
